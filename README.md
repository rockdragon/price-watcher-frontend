# Price Watcher（プライス・ウォッチャー）のフロントエンド

## 事前準備
1. <code>.env.development</code>を作る
```editorconfig
NEXT_PUBLIC_SERVER_HOST=http://localhost:8080
```

2. Node.js パッケージをインストールする
```bash
npm install
```

## 起動する方法
```bash
./run.sh
```